import React from "react";

const Logo = () => {
  return (
    <div className="mx-auto text-[52px]">
      {true ? `ανιѕ мє∂ια` : `𝔄𝔳𝔦𝔰 𝔐𝔢𝔡𝔦𝔞`}
    </div>
  );
};

export default Logo;
